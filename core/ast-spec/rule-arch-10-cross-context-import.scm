;; rule-arch-10-cross-context-import
;; Tree-sitter query for this rule is NOT POSSIBLE because it depends on file name and directory context.
;; Use Semgrep rule in .semgrep.yaml for cross-context import detection.
;; See ADR-0026: Import path ../<contextB>/* outside port.ts/index.ts re-exports.
;;
;; This file provides a comment-only header as a placeholder.
;; The rule is implemented via Semgrep (path-based exclusion of port.ts/index.ts).
